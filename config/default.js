const config = {
    //启动端口
    port: 3012,
    database: {
        DATABASE: "nodesql",
        HOST: "localhost",
        USERNAME: "root",
        PASSWORD: "root",
        port: "3306"
    }
}

module.exports = config;